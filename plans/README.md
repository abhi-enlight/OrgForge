# Animation Plans — Forge frontend

Generated by the `improve-animations` audit (Emil Kowalski playbook, 8 categories) on 2026-08-12. Scope: `frontend/src` (Next.js 16 + React 19 + Tailwind v4 + framer-motion 11). Plans are self-contained — any agent with zero context can execute one.

Not a git repository at stamp time, so no plans carry a commit hash; if a repo appears, stamp with `git rev-parse --short HEAD` before executing.

## Plan index

| # | Title | Severity | Category | Status |
|---|-------|----------|----------|--------|
| [001](./001-toast-slide-in-and-exit.md) | Give toasts a real entrance and exit | HIGH | Craft / interruptibility | DONE |
| [002](./002-scope-hover-transitions.md) | Scope `transition-all` to the properties that change | MEDIUM | Performance | DONE |
| [003](./003-reduced-motion-global-gating.md) | Close the app-wide reduced-motion gaps | MEDIUM | Accessibility | DONE |
| [004](./004-gate-smooth-scroll.md) | Gate `scroll-behavior: smooth` behind reduced motion | MEDIUM | Accessibility | DONE |
| [005](./005-motion-token-consolidation.md) | Consolidate easing into one shared token module | MEDIUM | Cohesion & tokens | DONE |
| [006](./006-dropdown-origin-and-duration.md) | Anchor dropdown scale to the trigger, tighten to 200ms | MEDIUM | Physicality & origin | DONE |
| [007](./007-transform-only-shimmer.md) | Make the skeleton shimmer transform-only | MEDIUM | Performance | DONE |
| [008](./008-scaleX-progress-bars.md) | Animate LivePipeline bars with scaleX, not width | MEDIUM | Performance | DONE |
| [009](./009-org-selector-dropdown-entrance.md) | Org selector dropdown entrance (cohesion with header menus) | LOW | Missed opportunity | DONE |
| [010](./010-sidebar-backdrop-fade.md) | Fade the mobile sidebar backdrop | LOW | Missed opportunity | DONE |
| [011](./011-reduced-motion-typing-indicator.md) | Freeze the chat typing indicator under reduced motion | MEDIUM | Accessibility | DONE |
| [012](./012-scope-transition-all-primitives.md) | Scope `transition-all` in the core UI primitives | MEDIUM | Performance | DONE |
| [013](./013-scope-transition-all-batch.md) | Scope the remaining `transition-all` batch (app surfaces) | MEDIUM | Performance | DONE |
| [014](./014-scope-landing-cta-transitions.md) | Scope the landing CTA `transition-all` (Hero, Nav, CtaSection) | LOW | Performance | TODO |

## Recommended execution order (post-010 re-audit batch)

11. **011** — reduced-motion typing indicator. Independent, one-line CSS kill-list addition (completes plan 003's accessibility gap).
12. **012** — primitives `transition-all` scoping (Button/Card/Input/Badge). Independent; highest-frequency elements first.
13. **013** — remaining `transition-all` batch (10 app files). Independent of 012 (disjoint files) but same pattern — run after 012 to keep the diff story clean.

No dependencies among 011–014 or on 001–010. 013 and 014 are the same scoping pattern on disjoint files — either order; 014 is the trivial last batch.

## Recommended execution order

1. **003** — global `MotionConfig reducedMotion="user"` + CSS/Sidebar gating. Foundational: makes reduced-motion feel-checks for every later plan pass, and it's a no-op for everyone else. No dependencies.
2. **004** — smooth-scroll gating. Independent, tiny.
3. **006** — dropdown origin + 200ms. Independent of 005 (uses the CSS token directly).
4. **007** — transform-only shimmer. Independent.
5. **008** — scaleX progress bars. Independent; uses the app curve inline (005 optional).
6. **002** — `transition-all` scoping. Independent (class strings only).
7. **009** — OrgSelector entrance. Depends on 006's pattern (uses `animate-scale-in origin-top-right`); safe to do after 006.
8. **010** — sidebar backdrop fade. Independent; pairs visually with 003's drawer gating.
9. **005** — token consolidation. Runs late on purpose so earlier plans (008, 001) can inline values it then unifies; must run **before** 001 only if 001 hasn't landed yet.
10. **001** — toast entrance + exit. **Depends on 005** (should import `EASE_OUT`) and **depends on 003** (reduced-motion compliance). Do last.

## Dependencies at a glance

- 001 → 003 (reduced motion), 005 (`EASE_OUT`)
- 009 → 006 (shared dropdown treatment)
- 005 → nothing, but touches files 001/008 use — run it after them (or coordinate)
- 002, 004, 007, 008, 010 → none

## File-conflict notes

- `globals.css`: 004, 006, 007 all edit it — non-overlapping rules (html block / `.animate-scale-in` / `.skeleton`), safe in any order.
- `Sidebar.tsx`: 003 (drawer `motion-reduce:transition-none`) and 010 (backdrop) — different blocks, safe together.
- `ToastProvider.tsx`: only 001 touches it.
- `Header.tsx`: only 006.

## Post-audit fixes (not numbered plans)

- **Re-audit missed opportunity #1 — grid stagger** (2026-08-12): the agent-card grid (`agents/page.tsx:424`) and the three dashboard stat tiles (`dashboard/page.tsx:217`) now cascade in with a 40ms per-item stagger, `y: 10` rise, `EASE_REVEAL`, reduced-motion gated — the StageTimeline pattern.
- **ErrorBanner expand** — **FIXED (2026-08-12)**: now uses the `grid-template-rows: 0fr → 1fr` trick (`ErrorBanner.tsx:161-164`) with a `min-h-0 overflow-hidden` child, reduced-motion gated (`initial={reduceMotion ? false : …}`), and the shared `EASE_OUT` token — closing the re-audit notes (layout animation, missing reduce gate, default ease). Typecheck + lint clean, code review clean.

## Deferred (audited, not planned)

None — every audited item is now either a numbered plan (001–014) or a completed post-audit fix (grid stagger, ErrorBanner expand).
